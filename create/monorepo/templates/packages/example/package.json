{
  "name": "@{{namespace}}/example",
  "version": "{{version}}",
  "source": "src",
  "main": "lib",
  "devDependencies": {
    "@{{namespace}}/builder": "^{{version}}"
  },
  "scripts": {
    "prepare": "{{namespace}}-builder",
    "start": "{{namespace}}-builder"
  }
}