{
  "name": "{{scoped-namespace}}-example",
  "version": "{{version}}",
  "source": "src",
  "main": "lib",
  "devDependencies": {
    "{{scoped-namespace}}-builder": "^{{version}}"
  },
  "scripts": {
    "prepare": "{{namespace}}-builder",
    "start": "{{namespace}}-builder"
  }
}